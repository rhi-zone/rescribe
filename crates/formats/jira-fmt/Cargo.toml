[package]
name = "jira-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Jira wiki markup parser, AST, and builder — no rescribe dependency"
