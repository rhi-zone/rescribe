[package]
name = "bbcode-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "BBCode parser, AST, and builder — no rescribe dependency"
