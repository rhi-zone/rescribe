[package]
name = "ansi-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ANSI terminal text parser, AST, and builder — no rescribe dependency"
