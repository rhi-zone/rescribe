[package]
name = "zimwiki"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ZimWiki parser, AST, and builder — no rescribe dependency"
