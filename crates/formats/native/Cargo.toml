[package]
name = "native"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "rescribe native format parser and builder — no rescribe dependency"
