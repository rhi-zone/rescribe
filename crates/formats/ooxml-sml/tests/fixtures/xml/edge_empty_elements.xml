<?xml version="1.0" encoding="UTF-8"?>
<!-- Edge case: empty and self-closing elements -->
<worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main">
    <dimension ref="A1:C3"/>
    <!-- Empty sheetViews -->
    <sheetViews>
        <sheetView workbookViewId="0"/>
    </sheetViews>
    <sheetData>
        <row r="1">
            <!-- Empty cell (self-closing) -->
            <c r="A1"/>
            <!-- Cell with empty value -->
            <c r="B1"><v></v></c>
            <!-- Cell with style but no value -->
            <c r="C1" s="1"/>
        </row>
        <!-- Empty row (self-closing) -->
        <row r="2"/>
        <row r="3">
            <c r="A3"><v>Data</v></c>
        </row>
    </sheetData>
    <!-- Empty mergeCells with count 0 -->
    <mergeCells count="0"/>
</worksheet>
