[package]
name = "vimwiki-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "VimWiki parser, AST, and builder — no rescribe dependency"

[dependencies]
