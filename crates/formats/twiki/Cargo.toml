[package]
name = "twiki"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "TWiki parser, AST, and builder — no rescribe dependency"
