[package]
name = "muse-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Muse parser, AST, and builder — no rescribe dependency"
