[package]
name = "format-crate-trait"
version = "0.1.0"
edition = "2024"
publish = false
description = "EXPLORATORY SPIKE — not part of the public API surface. See src/lib.rs."

[dependencies]
opml-fmt = { workspace = true }
zip-fmt = { workspace = true }
