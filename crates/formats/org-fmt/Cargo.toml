[package]
name = "org-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Org-mode parser, AST, and builder — no rescribe dependency"
