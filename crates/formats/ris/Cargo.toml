[package]
name = "ris"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "RIS citation format parser, AST, and builder — no rescribe dependency"
