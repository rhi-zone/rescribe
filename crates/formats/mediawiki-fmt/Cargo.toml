[package]
name = "mediawiki-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "MediaWiki markup parser, AST, and builder — no rescribe dependency"
