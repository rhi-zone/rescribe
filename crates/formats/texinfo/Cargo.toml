[package]
name = "texinfo"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Texinfo parser, AST, and builder — no rescribe dependency"
