[package]
name = "pod-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "POD (Plain Old Documentation) parser, AST, and builder — no rescribe dependency"
