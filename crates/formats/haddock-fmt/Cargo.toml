[package]
name = "haddock-fmt"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Haddock documentation parser, AST, and builder — no rescribe dependency"
