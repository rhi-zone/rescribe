[package]
name = "rescribe-read-commonmark"
version = "0.1.0"
edition = "2024"

[dependencies]
rescribe-core = { path = "../../rescribe-core" }
rescribe-std = { path = "../../nodes/rescribe-std" }
rescribe-format-api = { path = "../../formats/rescribe-format-api" }
commonmark-fmt = { path = "../../formats/commonmark-fmt", features = [
    "frontmatter",
    "tables",
    "task-lists",
    "strikethrough",
    "footnotes",
    "definition-lists",
    "math",
] }
serde_yaml.workspace = true
toml.workspace = true
