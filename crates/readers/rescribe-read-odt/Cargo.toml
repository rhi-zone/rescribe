[package]
name = "rescribe-read-odt"
version = "0.1.0"
edition = "2024"

[dependencies]
rescribe-core = { path = "../../rescribe-core" }
rescribe-std = { path = "../../nodes/rescribe-std" }
odf-fmt = { path = "../../formats/odf-fmt" }
# Used by the gen_fixtures binary tool.
zip = "2"

[dev-dependencies]
zip = "2"
