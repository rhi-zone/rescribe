# Rust
/target/

# Nix
result
.direnv/

# Node (docs)
node_modules/
docs/.vitepress/cache/
docs/.vitepress/dist/

# Spec downloads (large PDFs, but keep our config YAMLs and fixture specs)
/spec/*
!spec/*.yaml
!spec/fixtures/
!spec/fixtures/*.yaml

# External corpora (NapierOne, docx-corpus, etc.)
# These are large and downloaded separately - see ECOSYSTEM.md
/corpora/
*.zip

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Normalize
.normalize/*
!.normalize/config.toml
!.normalize/duplicate-functions-allow
!.normalize/duplicate-types-allow
!.normalize/hotspots-allow
!.normalize/large-files-allow
!.normalize/memory/
