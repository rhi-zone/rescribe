{
  "description": "A non-standard entry type (@preprint, not in biblatex::EntryType's known vocabulary) is captured verbatim as biblatex:type. Previously this used biblatex::EntryType's Debug rendering of the Unknown variant (\"unknown(\\\"preprint\\\")\") instead of the literal name — fixed to match rescribe-read-bibtex's entry_type_string handling",
  "category": "rare",
  "assertions": [
    { "path": "/0",   "kind": "definition_list" },
    { "path": "/0/0", "kind": "biblatex:entry" },
    { "path": "/0/0/1", "kind": "definition_desc", "props": { "biblatex:key": "ds2024", "biblatex:type": "preprint" } }
  ]
}
