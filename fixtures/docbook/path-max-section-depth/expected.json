{
  "description": "15 levels of nested <section> (DocBook's <section> is recursive with no schema-enforced depth limit, unlike the fixed sect1-sect5 hierarchy) parse without excessive slowdown or stack issues",
  "category": "pathological",
  "assertions": [
    { "path": "/0",   "kind": "div" },
    { "path": "/0/0", "kind": "div" },
    { "path": "/0/0/0", "kind": "heading", "props": { "level": 2 } }
  ]
}
