{
  "description": "[%hardbreaks] block option is not recognized; the % prefix causes the attribute to be silently ignored and the following content is parsed as a normal paragraph with lines joined",
  "category": "rare",
  "assertions": [
    { "path": "/0", "kind": "paragraph" },
    { "path": "/0/0", "kind": "text", "props": { "content": "First line Second line" } }
  ]
}
