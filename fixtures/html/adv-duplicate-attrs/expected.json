{
  "description": "duplicate attributes on the same element are handled without panic; first value wins",
  "category": "adversarial",
  "assertions": [
    { "path": "/0", "kind": "paragraph", "props": { "id": "a" } }
  ]
}
