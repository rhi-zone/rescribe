{
  "description": "cols and rows attributes on a table cell are preserved as tei:cols / tei:rows",
  "assertions": [
    { "path": "/0/0/0", "kind": "table_cell", "props": { "tei:cols": "2", "tei:rows": "1" } }
  ]
}
