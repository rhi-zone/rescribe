{
  "description": "unclosed <p> tag: parser recovers via mismatched-tag diagnostics without panicking",
  "category": "adversarial",
  "assertions": [
    { "path": "/0", "kind": "paragraph" }
  ]
}
