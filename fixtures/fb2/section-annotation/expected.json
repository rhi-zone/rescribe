{
  "description": "a section-level <annotation> (Section.annotation, distinct from title-info's) must not leak its content into the section's own paragraphs, nor be dropped from fb2-fmt's events()/streaming_writer output",
  "category": "happy",
  "assertions": [
    { "path": "/0",       "kind": "div" },
    { "path": "/0/0",     "kind": "div" },
    { "path": "/0/0/0",   "kind": "paragraph" },
    { "path": "/0/0/0/0", "kind": "text", "props": { "content": "Main content." } }
  ]
}
