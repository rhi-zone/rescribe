{
  "description": "GFM table syntax inside a blockquote — commonmark-fmt does not implement GFM tables; pipe syntax is parsed as paragraph text",
  "category": "integration",
  "assertions": [
    { "path": "/0", "kind": "blockquote" }
  ]
}
