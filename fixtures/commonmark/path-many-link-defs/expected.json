{
  "description": "200 link reference definitions — parser must resolve all references without blowing up",
  "category": "pathological",
  "assertions": [
    { "path": "/0", "kind": "paragraph" }
  ]
}
