{
  "description": "Automatic style with text properties: bold=true, color=#cc0000, font-size=14pt. Known gap: inline_kind_from_style's bold/italic/underline/strikeout branches take priority over the color/size span branch, so a run that is bold AND colored AND sized currently only produces a strong node — color/size are dropped, not raw-preserved. Tracked in TODO.md/COVERAGE.md rather than fixed here (pre-existing, shared with fixtures/odt/, out of this session's ADR-0015 scope).",
  "category": "happy",
  "assertions": [
    { "path": "/0", "kind": "paragraph", "props": { "odt:style-name": "Default" } },
    { "path": "/0/0", "kind": "strong" },
    { "path": "/0/0/0", "kind": "text", "props": { "content": "Bold red text" } }
  ]
}
