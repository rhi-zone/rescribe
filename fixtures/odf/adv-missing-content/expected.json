{
  "description": "Valid ZIP archive missing content.xml — parser must not panic; degrades to an empty body rather than erroring",
  "category": "adversarial",
  "assertions": [
    { "path": "", "kind": "document", "children_count": 0 }
  ]
}
