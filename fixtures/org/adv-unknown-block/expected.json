{
  "description": "Unknown block types are emitted as div nodes; must not panic",
  "category": "adversarial",
  "assertions": [
    { "path": "/0", "kind": "div" },
    { "path": "/1", "kind": "paragraph" }
  ]
}
