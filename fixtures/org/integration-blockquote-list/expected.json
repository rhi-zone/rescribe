{
  "description": "#+BEGIN_QUOTE containing list markers produces blockquote with a paragraph (inline parsing only); must not panic",
  "assertions": [
    { "path": "/0",   "kind": "blockquote" },
    { "path": "/0/0", "kind": "paragraph" }
  ]
}
