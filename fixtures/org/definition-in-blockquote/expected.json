{
  "description": "Definition list inside a blockquote: the QUOTE block content is parsed as inline text only (not re-parsed for blocks), so the definition list syntax appears as literal text in the blockquote paragraph",
  "assertions": [
    { "path": "/0", "kind": "blockquote" },
    { "path": "/0/0", "kind": "paragraph" },
    { "path": "/0/0/0", "kind": "text", "props": { "content": "- term :: desc" } }
  ]
}
