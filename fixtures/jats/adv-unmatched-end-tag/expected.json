{
  "description": "a stray closing tag with no corresponding open tag at all (</p> after the document's own root has already closed) does not panic the parser; it reports a diagnostic rather than being silently accepted",
  "category": "adversarial",
  "assertions": []
}
