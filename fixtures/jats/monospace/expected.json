{
  "description": "<monospace> maps to the standard code inline node, tagged jats:tag=\"monospace\" so the writer re-emits <monospace> rather than <code>",
  "assertions": [
    { "path": "/0",     "kind": "div" },
    { "path": "/0/0",   "kind": "paragraph" },
    { "path": "/0/0/0", "kind": "code", "props": { "content": "ls -la", "jats:tag": "monospace" } }
  ]
}
